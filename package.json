{
  "name": "sensaura-ai",
  "version": "1.0.0",
  "description": "SensAura AI - Offline-First Ambient Intelligence Layer",
  "private": true,
  "scripts": {
    "build": "bash vercel-build.sh",
    "vercel-build": "bash vercel-build.sh",
    "build:local": "flutter build web --release",
    "deploy:prebuilt": "npx vercel deploy build/web --prod",
    "deploy:prod": "npx vercel --prod"
  }
}
